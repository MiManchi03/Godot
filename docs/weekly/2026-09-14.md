# 每周 Harness 维护周报 — 2026-09-14

> 首次周报；健康度日志从本周开始记录，**暂无上周对比基线**。
> 产出脚本：`scripts/harness_health.sh`；数据：`docs/harness-health.log`

## 1. 健康度快照

| 指标 | 数值 | 解读 |
|---|---|---|
| verify.sh | PASS / 7s | 体系存活 |
| 测试 | 总数 17 / 本月新增 17 / 跳过 0 | Phase 4 从 0 起步 |
| PITFALLS | 本月 +7；标签口径 6/7 自动 | ⚠️ 口径偏乐观，见 §3 |
| 重复错误模式 | 🔴 契约失修/失配 2 次 | 同族问题，建议一次性收口 |
| 活文档新鲜度 | STATUS 0 天 / 最近 commit 0 天 | 健康 |
| Spec 覆盖 | 9/10；裸奔 1（effects） | 见 §5 |
| 规则膨胀 | AGENTS 38 行 / agent 72 行 | 阈值内 |

趋势：本周即基线，下次周报起可对比。可关注的长期信号是「重复错误模式」与「裸奔数」两项。

## 2. 本周 git log 审计（19 次提交）

- **STATUS.md 漏更新：无。** Phase 3–7 每次均同批更新（`a63b427` / `d531ab6` / `c268892` / `d7c2b6e` / `ae2c526`）。
- **超 spec 改动：无。** 本周唯一游戏脚本改动是 `7909065`（VillageService 非法语法修正，属修复而非新增功能），其余 18 次均为 Harness 自身。
- 未提交 WIP：`villager_system.gd`、`player_controller.gd`、`world_manager.gd`（你的进行中改动，不在本次审计范围）。

## 3. PITFALLS 未自动化项与方案

| 条目 | 现有标签 | 真实状态 | 建议 |
|---|---|---|---|
| pre-commit 暂存丢改动 | [自动] | 半自动：仅移除了闯祸的本地钩子，机制本身无守卫 | 降级为「半自动」 |
| gdtoolkit 超大脚本 | [自动] | 缓解而非拦截（跳过 >1MB） | 降级为「缓解」 |
| 术语表子串冲突 | [自动] | **未自动化**：无任何检测 | 方案 A（见下） |
| 契约声明未提交接口 | 半自动 | 半自动（诚实） | 方案 B（见下） |

**方案 A — 术语子串冲突自动检测**（约 20 分钟）
在 `terminology_registry.load()` 增自检：任一 `forbidden` 词若是同表某 `aliases/zh_label/canonical` 的子串即报错；pre-commit 的术语钩子顺带生效。

**方案 B — 契约 `pending` 标记**（约 15 分钟）
契约方法条目支持 `"pending": true`；`validate_contracts.py` 对「pending 且脚本中缺失」只报警告不报错。用于 WIP 尚未提交时的接口占位。

## 4. 抽查：resource_spawner 的测试是否真测 AC

- 对照 `SPEC.md` 3.3：平原 70/20/10、森林 60/30/10、山地 50/30/20。
- `tests/unit/test_resource_spawner.gd` 逐项断言与之一致（4000 采样、固定随机种子、容差 0.05）。
- **反证（本次现场执行）**：把平原草丛 0.70 改成 0.30 → `test_plains_distribution_matches_spec` 失败（exit=1）；改回后消失。→ 测试确实绑定 AC，**不是为通过率而写的假测试**。
- 小瑕疵：`test_attempt_count_per_biome` 断言的 20/25/18 并不在 SPEC 3.3 中（实现细节外溢）。建议要么写进 spec，要么降级为注释。

## 5. 其他观察（非决策）

- `village_service.gd` 本周被改（语法修正）却**零测试**；它承载入住 / 搬离业务，建议在 WIP 落地时补首批单测。
- `effects/building_highlight.gd` 仍无 spec（裸奔）。

## 6. 需要你决策（3 件）

1. **方案 A**：术语子串冲突自动检测 —— 做 / 不做？
2. **方案 B**：契约 `pending` 标记支持未提交接口 —— 做 / 不做？
3. **口径修正**：把 PITFALLS 的 `[自动]` 标签诚实化，并让健康度只统计「真拦截」—— 做 / 不做？
